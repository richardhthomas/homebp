<div id='main'>
	
	<div id='strip1'>
		<div id='strip1_content'>
			<% if !user_signed_in? %>
				<h2>The best blood pressure treatment</h2>
				<h3>FREE advice from UK GPs.</h3>
				<h4>Online. Anytime.</h4>
			<% end %>
		</div>
	</div>
	
	
	<div id='strip4'>
		<div id='strip4_content'>
			<div id='graphic'>
				<% if !@average_sysbp.nil? %>
					<div id='average_bp'>
						<%= render :partial => 'average_bp', :locals => {:avsys => @average_sysbp, :avdia => @average_diabp, :bppos => @bp_position} %>
					</div>
					<div id='reading_counter'>
						<%= render :partial => 'reading_counter' %>
					</div>	
				<% end %>
			</div>
			<div id='text'>
				<% if bp_warning_message(@current_average_bp) %>
				<p id='warning_msg'><%= bp_warning_message(@current_average_bp) %></p>
			<% end %>
	
			<% if !bp_warning_message(@current_average_bp) %>
				<p><%= average_bp_message(@average_sysbp, @average_diabp) %></p>
			<% end %>
	
			<p>...However, to get a more accurate picture of what your blood pressure is like you need to take more readings like this <span><%= time_left %></span>. This is because your blood pressure will vary from hour to hour, so one or two readings can be misleading. By taking more readings, you will get more accurate advice from this site.</p>
			
			<% if !user_signed_in? %>
				<p id='feature_1'>You can be guided through this process by us... we'll make it easy.</p>
				<p id='feature_2'>You can control your own blood pressure at home.</p>
			<% end %>
			</div>

		</div>
	</div>
	
	<div id='strip5'>
		<div id='strip5_content'>
			<% if user_signed_in? %>
				<p id='return_notice'>Please come back to HomeBloodPressure.co.uk <span><%= when_next_bp_reading(@current_average_bp.ampm) %></span> to take another pair of blood pressure readings.</p>
			<% else %>
				<%= render :partial => 'devise/registrations/new' %>
			<% end %>
		</div>
	</div>
		
</div>


