<%= form_for(@current_bp) do |f| %>
	<%= f.hidden_field :date, {:value => @bp_entry_details[:date]} %>
	<%= f.hidden_field :ampm, {:value => @bp_entry_details[:ampm]} %>
	<%= f.hidden_field :reading_no, {:value => @bp_entry_details[:reading_no]} %>
	<div id='input_bp_boxes'>
    	<%= f.text_field(:sysbp) %>
  		<div id='slash'>
  			/
  		</div>
	    <%= f.text_field(:diabp) %>
  		<div id='mmhg'>
  			mmHg
  		</div>
  	</div>
  	
  	<%= @bp_entry_details[:date]%>
  	
  	<% if @current_bp.errors.any? %>
  		<div id='error_wrapper'>
			<%= error_messages_for "current_bp", "sysbp" %>
		</div>
  	<% end %>
  	
  	<div id="submit">
	   	<%= f.submit 'Next', :class => 'button6' %>
	</div>
	
<% end %>